streamlit>=1.35.0
plotly>=5.22.0
# 2.1.0 minimum: dashboard.py uses Styler.map(), which was introduced in
# pandas 2.1.0 as the replacement for the now-removed Styler.applymap().
pandas>=2.1.0
# Tested with websockets 15.x; cap the next major API until revalidated.
websockets>=14.0,<16.0
# python-deriv-api removed: the project uses a custom raw WebSocket client
# (DerivAPIClient in src/api_client.py) and does not import python-deriv-api
# anywhere. Keeping it caused a dependency conflict with websockets>=14.
python-dotenv>=1.0.0
